# 已迁移至 analysis/deprecated_pipeline/field_detector.py (2026-07-24)
# 原因：gravity_focus 和 detect() 仅用于旧管线的多样性排序。
# 保留代码，重建脚本需要时从 analysis/deprecated_pipeline/ 导入。
raise RuntimeError("field_detector 已移出检索链路。请从 analysis/deprecated_pipeline/ 导入。")
